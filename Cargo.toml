[package]
name = "ravenlock"
version = "2.0.0"
edition = "2021"
authors = ["xtr4ng3"]
description = "Local integrity sentinel with baseline seal, canary files, live terminal mode and defensive reports."
license = "MIT"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
